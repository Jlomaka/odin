import { ReactNode } from "react";
interface IProps {
    className?: string;
    value: string;
    children?: ReactNode | string;
}
export declare const TabContent: ({ className, value, children }: IProps) => import("react/jsx-runtime").JSX.Element | null;
export {};
