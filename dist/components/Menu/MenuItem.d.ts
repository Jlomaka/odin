import { ReactNode } from "react";
interface IProps {
    className?: string;
    children: ReactNode;
}
export declare const MenuItem: ({ className, children }: IProps) => import("react/jsx-runtime").JSX.Element;
export {};
